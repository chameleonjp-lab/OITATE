# OITATE エージェント実行規則

この規則はリポジトリ全体へ適用する。作業前に `README.md`、`docs/IMPLEMENTATION_ORGANIZATION.md`、作業分野の正式仕様を読む。

## 文書の扱い

- ユーザーの現在の明示指示を最優先する。
- 担当モデル、引き継ぎ、承認、公開前レビューは `docs/IMPLEMENTATION_ORGANIZATION.md` を正式な参照先とする。
- ゲーム仕様、制作範囲、状態遷移、技術方針は `README.md` の文書一覧に示す正式な参照先へ従う。実装組織の文書はゲーム仕様を上書きしない。

## 標準担当

| 責任 | 実行設定 |
|---|---|
| 作品の最終責任、仕様・リスク・公開の承認 | ユーザー本人 |
| 企画、設計、受入条件、最終判断支援 | `model: gpt-5.6-sol` / `reasoning_effort: high` |
| 日常の実装と進行 | `model: gpt-5.6-luna` / `reasoning_effort: max` |
| 複雑な検査と不具合調査 | `model: gpt-5.6-luna` / `reasoning_effort: max` |
| 反復確認と記録 | `model: gpt-5.6-luna` / `reasoning_effort: max` |
| 原因不明、安全性、重大または不可逆な判断 | `model: gpt-5.6-sol` / `reasoning_effort: xhigh` |
| 公開前の独立レビュー | 実装担当とは別の `model: gpt-5.6-sol` / `reasoning_effort: high` |

モデルと推論量を指定できる場合は、表の正規名を完全一致で使う。別モデルを指定モデルとして扱わない。サブエージェントへモデルを明示する場合は、モデル上書きが有効になる履歴なしまたは範囲を限定した引き継ぎを使い、必要な前提を依頼文へ書く。

## 必須の進め方

1. Sol・Highが範囲、受入条件、リスク、実装単位を整理する。
2. ユーザーが明示的に実装開始を指示した後、Luna・Maxが実装する。
3. 1作業につき1つのDraft PRを使い、`main`へ直接変更しない。
4. Luna・Maxが検査、修正、再確認、記録を続ける。昇格条件に該当した場合は、安全な地点で止めてSol・Extra Highへ引き継ぐ。
5. 完了候補を、実装へ参加していない別のSol・Highが、実装時の履歴を引き継がない文脈で独立して確認する。重大な指摘がある場合、Luna・Maxは記録だけを行い、修正前にSol・Extra Highへ昇格する。方針確定後にLuna・Maxが修正し、別のSol・Highによる独立確認をやり直す。
6. Draft PRのReady化、マージ、公開は、それぞれユーザーの明示承認後に限る。

調査、計画、レビューの依頼は実装許可に含めない。昇格やレビュー合格も、仕様変更、マージ、公開の許可にはならない。

## 記録

各Draft PRへ、目的、対象範囲、受入条件、担当設定、変更内容、検査結果、既知の制限、残る危険、次に必要なユーザー承認を残す。判断や検査を行った担当は、根拠と未確認事項を区別して記録する。
